<div class="profileHeader">
    <h1>Add a profile</h1>
</div>


<form class="profileForm" action="#" method="POST">
    <div class="profileBgc">
    <p>Age</p>
    <input type="number" min="0" max="99" name="age" value="" placeholder="Age">
    <p>City</p>
    <input type="text" name="city" value="" placeholder="Which city are you in?">
    {{#if error}}
    <h2 class="errMsg">{{error}}</h2>
    {{else}}
    <p>Give us a URL</p>
    <input type="text" name="url" placeholder="Your URL" value="" pattern="https?://.*" title="Must include http:// or https://">
    {{/if}}
    <p>Click button to submit and move on to petition</p>
    <input type="hidden" name="_csrf" value="{{csrfToken}}">

    <button>Add details</button>
    <button>Skip</button>

    </div>
</form>